/** Views */
import main from '@/views/Main.vue';

const routes = [
    { 
        path: '/', component: main,
        meta: {
            title: 'Home Page - Example App'
        }
    },
];

export default routes;